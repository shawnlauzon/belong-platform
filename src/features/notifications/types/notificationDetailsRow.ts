import type { Database } from '@/shared/types/database';

// Type for the notification_details view row
export type NotificationDetailsRow = Database['public']['Views']['notification_details']['Row'];