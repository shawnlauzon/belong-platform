import { vi } from 'vitest';

// This file previously contained setupSupabaseMocks() but that function
// has been consolidated into ../../shared/__tests__/mockSetup.ts to eliminate duplication.
// Use: import { setupSupabaseMocks } from '../../shared/__tests__/mockSetup';

// If you need additional API-specific mock utilities, add them here.
